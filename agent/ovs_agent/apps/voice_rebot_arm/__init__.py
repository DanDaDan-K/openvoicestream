"""voice_rebot_arm — reBot B601-DM cartesian-waypoint voice agent (Phase A).

Empty package marker. The app, actuator and driver modules self-register
their actuator builder when imported (see ``rebot_actuator.py``).
"""

{
  "name": "rpi5-sensevoice",
  "description": "Raspberry Pi 5 (CPU-only): SenseVoice offline ASR (zh/en/ja/ko/yue) via sherpa-onnx OfflineRecognizer.from_sense_voice. ASR-only transcription profile (no TTS) — SenseVoice serves the offline /asr path; streaming Paraformer also loads for /asr/stream.",
  "asr_backend": "cpu.sherpa_asr",
  "execution_policy": {
    "mode": "concurrent"
  },
  "env": {
    "ENSURE_OFFLINE_ASR": "1",
    "OFFLINE_ASR_PROVIDER": "cpu",
    "STREAMING_ASR_PROVIDER": "cpu"
  }
}
